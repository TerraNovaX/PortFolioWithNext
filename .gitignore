# 📦 Ignorer le dossier `node_modules`
node_modules/

# ⚙️ Fichiers de configuration spécifiques à l’OS
.DS_Store
Thumbs.db

# 🗄️ Ignorer les fichiers de logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 🔐 Fichiers d’environnement contenant des variables sensibles
.env
.env.local
.env.development.local
.env.production.local

# 🛠️ Dossiers et fichiers générés automatiquement
.next/
out/
dist/
build/

# 📦 Fichiers de dépendances de package managers
package-lock.json
yarn.lock
pnpm-lock.yaml

# 🔍 Cache de l'éditeur (VSCode, WebStorm, etc.)
.vscode/
.idea/
*.iml

# 💾 Base de données SQLite (si utilisée)
*.sqlite
*.sqlite3

# 🔄 Cache de Prisma (si utilisé)
prisma/migrations/
prisma/dev.db
